name: secret-scan

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: "0 6 * * 1" # weekly full-history sweep, Mondays 06:00 UTC
  workflow_dispatch:

permissions:
  contents: read

jobs:
  gitleaks:
    uses: FlowMatrix-AI/.github/.github/workflows/gitleaks.yml@d12147b29fd7d2922b8d85b79d4b58592fcfd2e7 # main
